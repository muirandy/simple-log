package com.github.muirandy;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeLogResourceIT extends LogResourceTest {

    // Execute the same tests but in native mode.
}